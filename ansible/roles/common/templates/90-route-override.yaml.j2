network:
  version: 2
  ethernets:
    enp3s0:
      dhcp4: true
      dhcp4-overrides:
        use-routes: false
      routes:
        - to: default
          via: "{{ edge_private_ip }}"
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
